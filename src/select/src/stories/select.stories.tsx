import { Meta } from '@storybook/react';
import { Select } from './select';

export default {
  title: 'iVND component/Select',
  component: Select,
  tags: ['iVND', 'Select']
} as Meta;

export const SelectDefault = {
  args: {}
};
