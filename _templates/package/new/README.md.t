---
to: src/<%= name %>/README.md
---
## `iVND Control <%= name.replace(name[0], name[0].toUpperCase()) %>`

### Install

`npm i @ivnd/<%= name %>` or `yarn add @ivnd/<%= name %>`

### Usage

```
import { <%= name.replace(name[0], name[0].toUpperCase()) %> } from "@ivnd/<%= name %>"

<<%= name.replace(name[0], name[0].toUpperCase()) %> />

```

### Documentation

See full docs at: https://ivnd-control-uat.vndirect.com.vn
